Place your Windows icon file here as:

- `assets/app.ico`

When this file exists, both builders automatically use it:

- `scripts/build_windows_exe.py`
- `installer/OmniMLGUI.iss`

Recommended icon:

- multi-size `.ico` including at least `16x16`, `32x32`, `48x48`, `256x256`
